# Plaintext secrets — NEVER commit
.env
.env.local
.env.*.local
src/main/resources/.env
src/main/resources/.env.local

# OK to commit (encrypted)
!.env.sealed
!.env.example
!src/main/resources/.env.sealed

# Maven
target/
*.jar
*.war
.mvn/

# Node (sealed-env CLI installed locally lives here — runtime deps are Maven only)
node_modules/
package.json
package-lock.json
yarn.lock
pnpm-lock.yaml

# IDE
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# OS
.DS_Store
Thumbs.db

# Local DB
data/
*.h2.db
*.mv.db
*.trace.db
